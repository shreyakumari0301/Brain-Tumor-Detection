# Minimal requirements for deployment (without heavy ML packages)
# Use this if PyTorch causes deployment issues
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
streamlit>=1.28.1
opencv-python-headless>=4.8.1
pillow>=10.1.0
numpy>=1.24.3,<2.0.0
requests>=2.31.0

# Note: PyTorch and torchvision are excluded from this file
# Install them separately if needed:
# pip install torch torchvision

